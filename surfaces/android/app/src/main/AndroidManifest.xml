<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET: the agent CLIs reach the model API. FOREGROUND_SERVICE + WAKE_LOCK:
         keep the node server (and a running agent turn) alive when backgrounded.
         IGNORE_BATTERY_OPTIMIZATIONS: ask the user to exempt us so Android doesn't
         kill the server mid-session. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- VIBRATE: haptic feedback for the WebView (navigator.vibrate) - drawer/nav ticks and
         the success celebration buzz. Without it the WebView's Vibration API is a no-op. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- POST_NOTIFICATIONS (Android 13+): the backgrounded-approval alert (#53). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- RECORD_AUDIO: the composer's mic button (voice input) goes through the WebView's
         speech/getUserMedia, which the OS gates behind this runtime permission. File attach
         needs no permission: it uses the system picker (ACTION_GET_CONTENT / SAF), which
         grants per-file URI access without a storage permission. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- MODIFY_AUDIO_SETTINGS: install-time permission the WebView's audio capture path wants
         alongside RECORD_AUDIO (logcat warns without it). Keeps the mic route clean. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgentNet"
        android:usesCleartextTraffic="true">

        <!-- usesCleartextTraffic: the WebView loads http://127.0.0.1 (loopback only,
             never leaves the device), so plain HTTP to localhost is required. -->

        <!-- arm64 pointer-tagging note (Solana Seeker = MediaTek MT6878, Android 16):
             bionic tags heap pointers with an arm64 top-byte tag (TBI, e.g. 0xb4...).
             A proot that reads guest memory via PTRACE_PEEKDATA hits EIO on a *tagged*
             address ("ptrace(PEEKDATA): I/O error") — the kernel rejects tagged ptrace
             addresses, so proot's first execve fails with EFAULT and node never starts.
             Snapdragon/Android 9 never tagged, which is why they worked and Seeker did not.
             (Proven on-device: identical address reads fine untagged, EIO with a 0xb4 top byte.)

             FIX (shipped): we ship the Termux proot built with the process_vm accelerator
             (process_vm_readv/writev), which strips the top-byte tag — so it never hits the
             tagged-PEEKDATA path. See surfaces/android/scripts/build-assets.sh (fetches that
             proot + libtalloc/libandroid-shmem) and ServerManager.kt (LD_LIBRARY_PATH).
             NOTE: allowNativeHeapPointerTagging="false" does NOT work here — it only affects
             this app's process, while proot is a separate fork+exec'd bionic process. -->


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ServerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Notification action buttons (#53): Stop / Approve / Reject. Internal only. -->
        <receiver
            android:name=".NotifActionReceiver"
            android:exported="false" />

    </application>
</manifest>
