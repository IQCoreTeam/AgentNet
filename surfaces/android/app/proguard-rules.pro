# ProGuard rules for the release build. Minification is off (isMinifyEnabled = false),
# so this is intentionally empty — kept because build.gradle.kts references it. Add
# keep-rules here if R8/ProGuard is ever turned on (e.g. for the WebView JS interface,
# though we currently use no addJavascriptInterface — the WebView talks plain HTTP).
